# Repository Integration Tests

See [accounts infrastructure README](../../accounts/infrastructure/README.md) for details on repository integration tests.

These tests are skipped by default and require a real Supabase database to run.

To enable: `RUN_INTEGRATION_TESTS=true npm test`
